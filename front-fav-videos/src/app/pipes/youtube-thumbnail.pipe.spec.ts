import { YoutubeThumbnailPipe } from './youtube-thumbnail.pipe';

describe('YoutubeThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
